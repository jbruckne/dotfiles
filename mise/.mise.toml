# This file contains global settings for mise
# For more information, see https://mise.jdx.dev/configuration/

[tools]
# Global tool versions
nodejs = "20.19.2"
java = ["openjdk-17", "openjdk-21"]

[settings]
# Always use shims to ensure consistent PATH behavior
always_keep_download = true
always_keep_install = true
legacy_version_file = true
jobs = 4
